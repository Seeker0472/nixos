# TODOS

## overall

## waybar

- write some custom widget
- more action

## hyprland

- custom layout
- animation {bezier xxx,a,b,c,d ; animation = xxx ,a,b,xxx,xxx xxx}
- space 123 improvement(maybe only allow one window) -> needs to use IPC,bit hard to implement
- space cv improvement(back to previous workspace)`hyprctl activeworkspace | grep workspace | awk '{print $3}'`
- gestures {workspace_swipe ... }
- ncmpcpp

## other

- dark theme
- use `libinput-gestures` to bind multi-fingure swip(e.g. switch workspace....) -> emm... maybe consider `fusuma` for better fuctionality? e.g. move/resize window? or hyprland it's self functionality?
- `xdg.desktopEntries`
- `wf-recorder -g "$(slurp)"` or `gpu-screen-recorder`-> gpr accerated but only can capture entire window or one application?
- maybe replace omf(or pack it with nix)

## small fun at terminal

- asciiquarium
- nyancat
- cmatrix
- pipes.sh
- Android tethering
- devenv
- video editing -> blender

## 重构的要求

- 参考大佬的flakes
- 经可能参数化,保证鲁棒性,
- 可以简单的增加flake input或者机器
- 认真打包脚本,不要把所有东西全部用Homemanager放进PATH里面
- 研究:透明代理,配置mihomo,研究http proxy的细节
- inmutable


